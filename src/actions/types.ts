export enum ActionTypes {
    fetchTodos = "FETCH_TODOS_REQUEST"
}
//typescript doesnt care about a value for an enum. Default is 0;
//redux doesnt care as long as its a unique value for the action type